<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="KOTORModSync.InformationDialog"
        Title="Information" Width="460" Height="300" WindowStartupLocation="CenterScreen"
        Background="#000116"
        ExtendClientAreaToDecorationsHint="True">
    <Panel>
        <Border Margin="16" Padding="16" CornerRadius="8" Background="#010116" BorderBrush="#062766" BorderThickness="2">
            <Grid RowDefinitions="Auto,*,Auto">
                <TextBlock x:Name="TitleTextBlock"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontSize="18" FontWeight="Bold"
                           Text="{Binding Title}"
                           Margin="0,0,0,8"
                           Grid.Row="0" />

                <ScrollViewer Grid.Row="1" MaxHeight="180" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="InfoTextBlock"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Top"
                               FontSize="14"
                               TextWrapping="Wrap"
                               TextTrimming="None" />
                </ScrollViewer>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                    <Button Content="OK" Click="OKButton_Click" MinWidth="100" />
                </StackPanel>
            </Grid>
        </Border>
    </Panel>
</Window>